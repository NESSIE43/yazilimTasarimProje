classDiagram
    class charactersFeatures {
        +string name
        +string target
        +int health
        +int damage
        +charactersFeatures(name, target, health, damage)
        +attack()
    }

    